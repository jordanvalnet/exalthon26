---
path: (repères de doc IA)
url: https://github.com/jordanvalnet/code-dev-intel.ts
fetched_at: 2026-09-10T14:23:13Z
---
Repères cherchés (7) : `AGENTS.md`, `CLAUDE.md`, `.cursorrules`, `llms.txt`, `.mcp.json`,
un dossier `skills/` ou `.claude/`, `.github/copilot-instructions.md`.

Repères trouvés : **aucun**. Score 0 / 7.

Constaté par listing, sans lecture de fichier :

- racine : `.dockerignore`, `.github/`, `.gitignore`, `.npmignore`, `CHANGELOG.md`, `CONTRIBUTING.md`, `LICENSE`, `README.md`, `__tests__/`, `docker/`, `docs/`, `eslint.config.mjs`, `package.json`, `perf/`, `pnpm-lock.yaml`, `schemas/`, `scripts/`, `security/`, `services/`, `tsconfig.build.json`, `tsconfig.json`, `vitest.config.ts` — aucun des repères.
- `.github/` : `workflows/` seulement — pas de `copilot-instructions.md`.
- `docs/` contient un dossier `ai/` (fait constaté par listing) ; il ne fait pas partie des sept repères et n'entre pas dans le score. Aucun de ses fichiers n'a été lu.
