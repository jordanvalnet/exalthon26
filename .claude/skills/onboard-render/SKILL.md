---
name: onboard-render
description: Étape 3 seule - le metteur en page produit deck-<profil>.html à partir du cache avec bun run render, puis vérifie. Usage - /onboard-render owner/repo profil.
argument-hint: owner/repo profil
---
Arguments : `$ARGUMENTS`.
Exécute `onboard/agents/3-render.md` à la lettre. Termine par `bun run validate deck <cache> <profil>` et le compte rendu de `onboard/WORKFLOW.md`.
